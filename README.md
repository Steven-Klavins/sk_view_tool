# SkViewTool

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'sk_view_tool'
```

And then execute:

    $ bundle install

Or install it yourself as:

    $ gem install sk_view_tool

